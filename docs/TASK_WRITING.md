# Task Writing

## 목적
이 문서는 task 문서를 작성하거나 구현 후 갱신할 때 따라야 하는 규칙입니다.

## 기본 원칙

- task 문서는 현재 단계에서 필요한 결과와 검증 기준을 고정하는 문서입니다.
- task 문서에는 기본적으로 What, 즉 무엇이 필요한지를 적습니다.
- 구현 전 task 문서에는 How, 즉 내부 구현 방식, 파일 분리 방식, 함수 구조를 추측해서 적지 않습니다.
    - 단, 사용자의 요청에 How가 포함된 경우 해당 내용을 task 문서에 그대로 반영한다.
- 구현 후 갱신은 `작성 시점별 규칙`의 `구현 후`를 따릅니다.
- 사용자 요청과 현재 task 범위를 벗어나는 항목은 task 문서에 추가하지 않습니다.
- 새 task 문서를 생성하면 `docs/PLANS.md` 작업리스트의 해당 단계 `Task 문서` 칸도 생성한 task 문서 링크로 갱신합니다.

## 파일명 규칙

- task 문서 파일명은 `{단계 번호}_{영문 설명}.md` 형식을 사용합니다. (예: `01-1_cube_movement.md`)
- 단계 번호는 `docs/PLANS.md` 작업리스트의 단계 번호와 동일하게 맞춥니다.
- 영문 설명은 소문자와 언더스코어(`_`)만 사용하고, 띄어쓰기와 한글은 사용하지 않습니다.
- 문서 안의 제목(`# Task XX-X - ...`)은 한글로 자유롭게 작성해도 되며, 이 규칙은 파일명에만 적용됩니다.

## 작성 시점별 규칙

### 구현 전

- `구현 항목`에는 현재 task에서 달성해야 하는 결과만 적습니다.
- `범위 밖`에는 이번 task에서 일부러 다루지 않을 항목을 적습니다.
- `사전 전제`에는 이전 단계 산출물이 필요할 때만 실제 전제 대상을 적습니다.
- `수동 작업` 과 `결과 확인`은 실제 코드 구현 전에는 구체 내용을 작성하지 않습니다.
- 실제 이름을 알 수 없는 파일명, 액터명, Blueprint명, 위젯명, 맵명, 입력명, 버튼 텍스트, 안내 문구를 추측해서 구체화하지 않습니다.
- 요청에 버튼명, 위젯명, 액터명, Blueprint명, 입력명 같은 기존 요소 이름이 명시되어 있으면 해당 이름으로 프로젝트의 코드와 자산 구성을 확인해 이미 존재하는지 검토한 뒤 구현 항목을 작성합니다.

### 구현 후

- 갱신은 실제 추가된 파일명, 액터명, Blueprint명, 맵명, 입력명, 위젯명 같은 구현 결과를 기준으로 합니다.
- 완료된 `구현 항목`과 `에이전트 확인` 체크박스를 실제 완료 범위만 `[x]`로 갱신합니다.
- `수동 작업`은 실제로 사람이 결과를 만들거나 진행시키기 위해 해야 하는 조작을 기준으로 구체화합니다.
- `결과 확인`은 실제 결과가 기대한 상태인지 사람이 확인할 수 있는 항목을 기준으로 구체화합니다.
- 사용자가 수동 작업 또는 결과 확인 완료를 응답하기 전에는 해당 확인 항목을 완료 처리하지 않습니다.
- 진행 중인 task에서 추가 변경이 발생해도 기존에 실제 완료된 항목은 유지하고, 새 변경으로 인해 새로 확인해야 하는 항목만 `[ ]`로 둡니다.

## 수동 작업과 결과 확인 공통 규칙

- 두 항목 모두 에이전트가 대신할 수 없어서 사람이 직접 해야 하는 일만 적습니다.
- 두 항목 모두 초보자가 그대로 따라 할 수 있도록 단계별로 구체적으로 적습니다.
- 구분 기준: 프로젝트에 변경을 남기는 조작(입력, 연결, 선택, 설정, 배치처럼 저장할 것이 생기는 일)은 `수동 작업`에, 아무것도 바꾸지 않고 실행하거나 보기만 하는 절차(맵을 열어 확인, PIE 실행 포함)는 `결과 확인`에 적습니다. (대체로 PIE 실행 전/후 구분과 일치)

## 수동 작업 작성 규칙

`수동 작업`에는 사람이 실행 전에 미리 준비해야 하는 항목만 적습니다.

아래는 대표적인 예시입니다.

- 프로젝트 설정에 직접 입력해야 하는 값의 종류
- 사람이 직접 선택하거나 연결해야 하는 모델, 애니메이션, 입력, 경로
- 사용할 참고 에셋 파일 배치

포함하지 않을 내용:

- 구분 기준상 `결과 확인` 소속인 실행·관찰 절차 (에디터 실행, 맵 열기, PIE 실행 등)
- 에이전트가 대신 수행할 코드 수정
- 현재 task와 무관한 다음 단계 준비
- 대상과 방법이 없는 추상 문장

### 좋은 예

- 사용할 모델 파일을 지정된 폴더에 직접 넣는다.
- 사용할 애니메이션 자산을 지정된 경로에 직접 연결한다.
- 입력 매핑에 사용할 키를 프로젝트 설정에서 직접 추가한다.

## 결과 확인 작성 규칙

`결과 확인`에는 실행 절차와 함께, 그 결과가 기대한 상태인지 확인하는 항목을 적습니다.

- `수동 작업`에 적은 입력, 연결, 설정 절차를 같은 의미로 반복하지 않습니다.

사람이 실행하고 눈으로 확인할 수 있는 모든 절차를 적습니다. 아래는 대표적인 예시입니다.

- Unreal Editor 실행
- 맵 열기
- PIE 실행
- 캐릭터 이동 입력
- 공격 입력
- 피격 반응 확인
- 체력 UI 확인
- 승리/패배 결과 확인

포함하지 않을 내용:

- 코드 내부 구조 설명
- 구현되지 않은 다음 단계 동작
- 현재 task 범위 밖의 통합 흐름

### 좋은 예

- Unreal Editor에서 대상 맵을 열었을 때 필요한 액터와 UI가 정상적으로 보이는지 확인한다.
- PIE를 실행했을 때 입력과 카메라 조작이 가능한지 확인한다.
- 상호작용 입력을 했을 때 기대한 상태 변화가 발생하는지 확인한다.
- 애니메이션 또는 이펙트가 필요한 타이밍에 정상적으로 보이는지 확인한다.
- 완료 또는 실패 조건이 충족됐을 때 결과가 올바르게 보이는지 확인한다.
